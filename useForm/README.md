# useForm Hook

Usage example:
```
    const initialForm = {
        id: '',
        name: '',
        email: ''
    }
    
    const { formValues, handleImputChange, reset } = useForm( initialForm );
```
